#pragma once
#include "Construction.hpp"

/// \brief return the name of a file chosen by the user
/// \param load : to know if it's a file to load, or to save
/// \param path : the string of the path
/// \param name : the string of the name
std::string name_file(const unsigned int load, std::string path, std::string name);


